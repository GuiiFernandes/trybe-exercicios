version: "3"

services:
  db:
    image: mysql:latest
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/var/lib/mysql
    ports:
      - 3306:3306
    container_name: library_db